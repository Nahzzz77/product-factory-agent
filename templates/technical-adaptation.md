# 技术适配声明

## 1. 产品形态

- 产品名称：`<产品名称>`
- 目标用户与核心任务：`<引用 PRD 章节，并说明用户要完成的任务>`
- 终端与当前阶段：Web 新建项目；仅覆盖 `<阶段 ID>` 的可验证最小切片。

## 2. 选定开发路径

- 选择路径：`<核心能力优先 / Web 前端切片 / 其他已批准路径>`。
- 选择理由：`<为何该路径直接验证 PRD 的核心任务>`。
- 当前不做：`<明确延后能力及不影响本阶段验收的理由>`。

## 3. 采用的默认基线

- 语言与运行时：`<实际版本与锁定方式>`。
- 后端、前端、存储与测试方案：`<实际选择；未采用时写不适用原因>`。
- 依赖与外部服务：`<本阶段实际依赖；不得在此写入凭据>`。

## 4. 触发的模块

- 核心流程模块：`<输入、处理、输出和持久化边界>`。
- 最小验收界面或自动化入口：`<需要与否及理由>`。
- 人工确认点：技术适配审批、`<其他 PRD 要求的确认点>`。

## 5. 偏离与强基线检查

- 与技术手册默认方案的偏离：`<无 / 逐项说明理由、影响和回退方式>`。
- 已检查的强制底线：不暴露敏感凭据、输入边界、错误处理、可恢复数据、测试与人工验收。
- 兼容性与迁移：`<已有项目/数据影响；新项目可写无>`。

## 6. 成本与数据边界

- 外部调用与成本预算：`<数量、额度或明确为 0>`。
- 数据分类与保留：`<是否处理个人或敏感数据；最小化措施>`。
- 部署、平台和权限：`<本阶段是否不涉及；若涉及，列出单独审批需要>`。

## 7. 需要产品负责人决定的事项

- [ ] `<待确认的业务取舍、费用、范围或验收容忍度；无则写“无”>`

## 8. 审批前自检

- [ ] 已逐项对齐 PRD、当前阶段开发文档和手册强制底线。
- [ ] 未引入未批准的付费服务、外部调用或数据用途。
- [ ] 已提供本阶段可执行的验证与恢复路径。
