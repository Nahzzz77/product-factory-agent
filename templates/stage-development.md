# 阶段技术开发记录

## 阶段信息

- 阶段 ID 与名称：`<stage-id / 名称>`
- 对齐 PRD：`<章节与验收条目>`
- 阶段目标：`<本阶段结束时用户可验证的结果>`

## 范围

- 包含：`<功能、接口或界面切片>`
- 不包含：`<明确延后项>`
- 完成定义：`<自动化和人工条件>`

## 数据与状态

- 输入、输出和持久化数据：`<结构、所有权、保留边界>`
- 状态转换与中断恢复：`<状态、失败后如何继续>`
- 敏感数据处理：`<无 / 最小化和隔离方式>`

## 接口与实现

- 对外接口或等价自动化入口：`<请求、响应、错误语义>`
- 依赖与边界：`<本地模块、外部服务及 mock/不适用说明>`
- 关键实现决策：`<可复现的理由>`

## 测试与验收

- 测试用例：`<正常、空值、错误、中断、恢复与回归>`
- 验收步骤：`<非技术人员可执行的逐步操作>`
- 预期结果：`<逐项可观察结果>`

## 风险与交接

- 风险、限制与缓解：`<性能、成本、隐私、兼容性或无>`
- 证据位置：`<命令、日志摘要、产物路径；不写入凭据>`
- 交接与下一步：`<待审批项、恢复命令、后续阶段输入>`
